[theme]
base = "dark"
primaryColor = "#5eeac0"
backgroundColor = "#0b1020"
secondaryBackgroundColor = "#141b30"

[server]
headless = true

[client]
# Dölj utvecklarverktygen (Fork / GitHub / meny) för betraktare — appen delas internt.
toolbarMode = "minimal"
